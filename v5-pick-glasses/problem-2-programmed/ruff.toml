extend = "../ruff.toml"

[lint.isort]
# The modules in this folder import each other by bare name.
known-first-party = ["find", "measure", "render", "run", "scoring", "views"]
known-third-party = ["work_cell"]
